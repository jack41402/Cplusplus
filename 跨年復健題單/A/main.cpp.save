#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int N , a , b , c ;
    cin >> N ;
    for 
    cin >> a >> b >> c ;
    cout << max(abs(a-b)+abs(a-c)+abs(b-c) , 0) << endl ;
    return 0;
}
