#include <iostream>

using namespace std;

long long int arr[1600005] ;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    long long int n , temp , ans=0 ;

    cout << ans << endl ;
    return 0;
}