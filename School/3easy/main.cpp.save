#include <iostream>

using namespace std;

int main()
{
	int N=8 , arr[N]={20, 18, 34, 79, 24, 95, 74, 13} , temp , i , j ;
	for(i=0 ; i<N-1 ; i++)
    {
        for (j=0 ; j<N-i-1 ; j++)
        {
            if (arr[j]>arr[j+1])
            {
                temp = arr[j] ;
                arr[j] = arr[j+1] ;
                arr[j+1] = temp ;
            }
        }
    }
    median = (arr[3]+arr[4]) / 2 ;
    cout << "最大值" << arr[7] << endl ;
    cout << "次小值" << arr[7] << endl ;
    cout << "" << median << endl ;
 	return 0;
}