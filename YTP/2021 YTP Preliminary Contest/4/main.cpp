#include <iostream>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    string str ;
    bool c ;
    long long int n , q , a , b ;
    cin >> n >> q >> str ;
    while (q--)
    {
        cin >> c ;
        if (c==1)
        {
            cin >>
        }
        else
        {
            str[b-1] = c ;
        }
    }
    return 0;
}
