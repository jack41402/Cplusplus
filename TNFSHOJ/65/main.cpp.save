#include <iostream>
#include <cstdlib>
#include <ctime>
#include <chrono>

using namespace std;

int main() {
    int a=chrono::system_clock::now().time_since_epoch().count()%2 ;
    if (a==0)
    {
        cout << 1 << endl ;
    }
    else
    {
        cout << 0 << endl ;
    }
    return 0;
}
